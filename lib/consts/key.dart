const REFRESH_TOKEN = 'refreshToken';
const ACCESS_TOKEN = 'accessToken';
const TOKEN = 'token';
const DIFY = 'dify';
const DATA = 'data';

const KB_REFRESH_TOKEN = 'kbRefreshToken';

enum Order {
  ASC,
  DESC,
}
